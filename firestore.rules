rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERMIT ALL READS (so site visitors can view properties)
      allow read: if true;
      // REQUIRE AUTH FOR WRITES (admin)
      allow write: if request.auth != null;
    }
  }
}
